## What changed?

Describe the problem and the solution.

## Verification

- [ ] `./gradlew testDebugUnitTest` (or Windows equivalent) passes
- [ ] `./gradlew assembleDebug` (or Windows equivalent) passes
- [ ] `git diff --check` is clean
- [ ] Real-device behavior was tested where the change requires it

## Safety / behavior review

- [ ] No secrets, cookies, private URLs, signing material, APKs, or local build state were committed
- [ ] Storage/export behavior remains safe and intentional
- [ ] Cancellation/failure paths were considered
- [ ] New permissions or dependencies are documented, if any
- [ ] Third-party notices were updated if bundled dependencies changed

## Screenshots

Add before/after screenshots for UI changes when useful.
